import 'package:flutter/material.dart';
import 'package:ionicons/ionicons.dart';

final class IconConstants {
  const IconConstants._();

  static const Widget facebook = Icon(Ionicons.logo_facebook);
  static const Widget google = Icon(Ionicons.logo_google);
  static const Widget apple = Icon(Ionicons.logo_apple);
  static const Widget back = Icon(Ionicons.chevron_back);
  static const Widget backspace = Icon(Ionicons.backspace);
  static const Widget discover = Icon(Ionicons.home);
  static const Widget matches = Icon(Ionicons.heart);
  static const Widget messages = Icon(Ionicons.chatbubble);
  static const Widget profile = Icon(Ionicons.person);
}
